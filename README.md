# Java String Tasks

This project contains solutions to 12 different Java String exercises.  


## What This Program Does

The program demonstrates how to:

- Get the length of a string.
- Find a character at a specific index.
- Search for a character inside a string.
- Extract substrings.
- Change text to lowercase and uppercase.
- Replacing the Words.
- Trim whitespace.
- Convert numbers to strings.
- Split strings into parts.
- Convert strings to char arrays.
- Convert char arrays back into strings.

Each task prints its result to the console when the program runs.



# Java Time API

This project contains solutions for 16 exercises using the Java Time API.
The program works with dates, times, formatting, and calculations such as adding years, finding weekdays, and comparing dates.
The project also includes a simple calendar method that prints all dates for the year 2018.

## What the Program Does

- Prints the current date

- Prints the current date in a formatted pattern

- Finds the most recent Monday and prints the whole week

- Creates a date from a string

- Prints the weekday of a birthday

- Adds and subtracts from the current date

- Calculates the period between two dates

- Adds a custom period to today’s date

- Prints the current time

- Prints nanoseconds of current time

- Creates time from a string

- Formats the current time

- Creates a LocalDateTime

- Formats the LocalDateTime

- Combines a date and a time

## Extracts the date and time separately from a LocalDateTime

Calendar Feature
The project also includes a simple calendar method that prints all months and all days in the year 2018, including the weekday for each date.
